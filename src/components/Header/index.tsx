import { HeaderContainer, HeaderContent } from "./style";
import { LogoImg } from "../LogoImg";

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderContent>
        <LogoImg />
      </HeaderContent>
    </HeaderContainer>
  );
};

export { Header };
